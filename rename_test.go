package melt

import "testing"

func TestRenameImportPath(t *testing.T) {

	type Case struct {
		Input     string
		Expected  string
		Old       string
		PrefixNew string
	}

	// tests := []Case{
	// }

	// for _, c := range tests {

	// 	if c.Expected != result {
	// 		t.Fatalf("failed case:\n Input:\t\t%s\n Expected:\t%s\n Output:\t%s\n", c.Input, c.Expected, result)
	// 	}
	// }
}
